you can just run `main.py` for now to see the whole flow
(this is also where you can replace the random data with some proper data)

all other files are runnable, i wrote a minimal `__main__` block for each of them
for some minor functionality testing
